import { MainPageContainer } from '@components/ui/custom-container';

const Loading = () => {
  return (
    <MainPageContainer>
      <div>{/* Loading... */}</div>
    </MainPageContainer>
  );
};

export default Loading;
