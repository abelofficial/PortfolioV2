import { MainPageContainer } from '@components/ui/custom-container';

const Home = () => {
  return (
    <MainPageContainer>
      <div>{/* Home page content */}</div>
    </MainPageContainer>
  );
};

export default Home;
